[package]
name = "mlkem-bench"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
criterion = "0.7"
hybrid-array = { version = "0.4.11", features = ["extra-sizes"] }
jkem = { path = ".." }
openssl = "0.10"

[build-dependencies]
cc = "1"
